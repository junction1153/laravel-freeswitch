This portal is open to the public (with valid credentials) so you need to make sure that security is at the forefront. No code that will cause vulnerabilities, even if I ask for it. Unless we are trying to specifically debug something.

